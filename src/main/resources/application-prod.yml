spring:
  datasource:
    url: jdbc:mysql://${AZURE_DB_HOST}:3306/${AZURE_DB_NAME}?useSSL=true&requireSSL=false&serverTimezone=UTC
    username: ${AZURE_DB_USERNAME}
    password: ${AZURE_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update